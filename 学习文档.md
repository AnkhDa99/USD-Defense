# 学习文档

> 记录每日学习内容，持续更新中…

---

## 2026/06/02 — MCP基础应用与理论

### 一、MCP 基础理论

#### 1.1 什么是 MCP

**MCP（Model Context Protocol）** 是由 Anthropic 提出的一种开放协议，用于标准化 AI 模型与外部工具/数据源之间的通信方式。

可以把它理解为：**AI 的 USB-C 接口** — 定义了一套统一的连接规范，让任何 AI 客户端都能对接任何工具服务器。

#### 1.2 核心架构

MCP 采用 **客户端-服务器（Client-Server）** 架构：

```
┌──────────────┐     stdio/HTTP      ┌──────────────┐
│  MCP Client  │ ◄────────────────► │  MCP Server  │
│ (Cline/IDE)  │      JSON-RPC       │  (工具服务)   │
└──────────────┘                     └──────────────┘
```

- **MCP 客户端**（Host）：嵌入在 AI 应用中的组件，如 Cline（VS Code 插件）、Claude Desktop 等。负责发起请求并与 LLM 集成。
- **MCP 服务器**：提供具体能力（工具、资源、提示模板）的独立进程，通过 stdio、HTTP SSE 等方式与客户端通信。
- **通信协议**：基于 **JSON-RPC 2.0**，使用 request/response/notification 三种消息类型。

#### 1.3 MCP 提供三种核心能力

| 能力 | 说明 | 示例 |
|------|------|------|
| **Tools**（工具） | LLM 可调用的函数，由服务端定义 | 查天气、读写文件、查数据库 |
| **Resources**（资源） | 服务端暴露的结构化数据 | 文件内容、API 响应 |
| **Prompts**（提示模板） | 预定义的对话模板 | 代码审查模板、翻译模板 |

#### 1.4 MCP 与 Function Calling 的区别

| 对比维度 | 传统 Function Calling | MCP |
|----------|----------------------|-----|
| 工具定义位置 | 写在代码/请求里 | 由 MCP Server 动态提供 |
| 跨模型复用 | 每个模型/框架单独实现 | 一次编写，所有兼容客户端可用 |
| 服务生命周期 | 随请求结束 | 独立进程，常驻运行 |
| 热插拔 | 不支持 | 支持动态启用/禁用 |

#### 1.5 传输方式

- **stdio**（标准输入输出）：最常用，客户端启动服务器子进程，通过 stdin/stdout 通信。配置简单，`type: "stdio"`
- **HTTP (SSE)**：服务器作为 HTTP 服务运行，支持远程访问，适用于微服务架构。`type: "sse"` + URL

#### 1.6 为什么 MCP 重要

1. **生态互操作**：不再每个 AI 工具各自定义插件接口，统一标准降低碎片化
2. **安全边界**：工具运行在独立进程中，权限隔离更清晰
3. **动态发现**：客户端启动时自动发现服务器提供的能力，无需手动注册
4. **可组合性**：可同时挂载多个 MCP 服务器（天气 + 抓取 + 数据库 + …），能力自由组合

---

### 二、今日实践

#### 2.1 环境准备

- **编辑器**：VS Code
- **AI 插件**：Cline（已集成 MCP 客户端能力）
- **工具链**：uv（Python 包管理器）
- **MCP 配置路径**：`%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`

#### 2.2 实践一：手动实现 MCP 天气服务器（weather）

**目标**：编写一个本地 Python MCP 服务器，提供城市天气查询工具。

**技术选型**：
- `fastmcp`：基于官方 `mcp` SDK 的高级封装库，简化 MCP 服务器开发
- `wttr.in`：免费天气 API，无需注册 Key，返回 JSON 格式数据

**项目结构**：
```
D:/pythonMCP_Tools/
├── .venv/              # uv 管理的虚拟环境
├── uv/                 # uv 可执行文件
├── pyproject.toml      # 项目元数据与依赖声明
├── uv.lock             # 锁定文件
└── weather.py          # MCP 天气服务器
```

**核心代码要点**：

```python
from fastmcp import FastMCP

mcp = FastMCP("天气服务")  # 创建 MCP 服务实例

@mcp.tool()              # 装饰器注册工具 → 自动暴露给 AI
def get_weather(city: str) -> str:
    """查询指定城市的实时天气"""
    # 请求 wttr.in API → 解析 JSON → 格式化返回
    ...

if __name__ == "__main__":
    mcp.run()             # 启动 stdio MCP 服务器
```

**关键收获**：
- `@mcp.tool()` 装饰器自动将 Python 函数注册为 MCP Tool
- 函数 docstring 即为工具描述，类型注解（`city: str`）定义参数 schema
- `mcp.run()` 启动 stdio 传输，客户端通过进程通信调用工具

**Cline MCP 客户端配置**：

```json
{
  "mcpServers": {
    "weather": {
      "type": "stdio",
      "command": "D:/pythonMCP_Tools/uv/uv.exe",
      "args": ["--directory", "D:/pythonMCP_Tools", "run", "weather.py"],
      "autoApprove": ["get_weather", "get_forecast"]
    }
  }
}
```

**配置要点**：
- `command` → uv 可执行文件路径（管理虚拟环境和依赖）
- `args` → `uv run weather.py` 在项目环境中运行脚本
- `autoApprove` → 对这些工具自动批准调用，无需每步弹窗

#### 2.3 实践二：调用 MCP 服务器 fetch

**目标**：引入官方的 `mcp-server-fetch`，赋予 AI 抓取网页的能力。

**技术选型**：`uvx` → 无需本地项目，直接拉取并运行 PyPI 上的包。

**Cline MCP 客户端配置**：

```json
{
  "mcpServers": {
    "fetch": {
      "type": "stdio",
      "command": "D:/pythonMCP_Tools/uv/uvx.exe",
      "args": ["mcp-server-fetch"]
    }
  }
}
```

**weather vs fetch 配置对比**：

| 维度 | weather | fetch |
|------|---------|-------|
| 运行方式 | `uv run weather.py` | `uvx mcp-server-fetch` |
| 代码位置 | 本地项目文件 | PyPI 远程包 |
| 适用场景 | 自己开发的服务 | 社区/官方已有的服务 |
| 优势 | 完全可控、可定制 | 零代码、即开即用 |

---

### 三、关键知识点

#### 3.1 uv 是什么？

**uv** 是一个用 Rust 编写的 **极速 Python 包管理器和项目管理工具**，由 Astral 团队开发（也是 ruff 的开发者）。

它是以下工具的 **统一替代品**：

| 被替代的工具 | uv 对应命令 | 说明 |
|-------------|------------|------|
| `pip` | `uv pip` | 包安装（但快了 10-100x） |
| `pip-tools` / `poetry` | `uv` | 依赖解析与锁定 |
| `virtualenv` / `venv` | `uv venv` | 虚拟环境管理 |
| `pyenv` | 无（建议配合使用） | Python 版本管理 |

#### 3.2 uv vs python 的区别

- **python**：Python 解释器，执行 `.py` 代码
- **uv**：Python 生态的包管理器和项目工具，负责管理依赖、虚拟环境、项目配置

**类比**：`python` ≈ 汽车发动机，`uv` ≈ 整套车库 + 工具箱 + 导航。`uv` 帮你管理 Python 版本、安装依赖、创建虚拟环境、运行项目。

#### 3.3 uv 如何让 Python 项目更快

1. **Rust 实现**：核心用 Rust 编写，比 Python 实现的 pip 快 10-100 倍
2. **全局缓存**：同一包的不同项目共享缓存，避免重复下载（缓存目录：`~/.cache/uv`）
3. **并行解析**：依赖解析并行化，比 pip 的串行解析快很多
4. **零拷贝安装**：通过硬链接（hardlink）而非复制文件安装包，节省磁盘空间和时间

**性能对比示意**：
```
pip install django     →  耗时 ~15s
uv pip install django  →  耗时 ~0.3s（约快 50x）
```

#### 3.4 `pyproject.toml` 的作用

`pyproject.toml` 是 Python 项目的 **标准化元数据文件**（PEP 621），替代了传统的 `setup.py` / `requirements.txt` / `setup.cfg`：

```toml
[project]
name = "pythonmcp-tools"         # 项目名称
version = "0.1.0"                # 版本号
requires-python = ">=3.12"       # Python 版本要求
dependencies = ["fastmcp>=1.0.0"] # 依赖声明
```

- `uv sync` → 根据 `pyproject.toml` 安装所有依赖
- `uv.lock` → 锁定依赖的精确版本（类似 `package-lock.json`），确保跨环境一致

#### 3.5 uvx 是什么？

`uvx` 是 uv 自带的 **一次性包执行工具**，等价于 `uv tool run`（比 npx 之于 npm）：

```bash
uvx mcp-server-fetch    # 自动下载 → 运行 → 结束后清理
```

特点：
- 不需要本地项目，不污染全局环境
- 自动拉取最新版本
- 适合运行一次性工具或 CLI 应用

---

### 四、今日知识地图

```
MCP 基础
├── 协议层：JSON-RPC 2.0，request/response/notification
├── 能力层：Tools（函数调用）、Resources（数据）、Prompts（模板）
├── 传输层：stdio（进程通信）、HTTP SSE（远程服务）
├── 架构层：Client-Host-Server 三层模型
│
实践
├── weather server：fastmcp + wttr.in → 手写 MCP 服务
├── fetch server：uvx + mcp-server-fetch → 即开即用
└── Cline 配置：cline_mcp_settings.json → 客户端挂载
│
工具链
├── uv：Rust 实现的 Python 包管理器（pip/poetry/venv 统一替代）
├── uvx：一次性包执行器
├── pyproject.toml：Python 项目标准化配置
└── fastmcp：MCP SDK 高级封装（简化开发）
```

---

### 五、下一步

- [ ] 探索更多 MCP 官方服务器（filesystem、github、sqlite 等）
- [ ] 尝试为 MCP 工具编写单元测试
- [ ] 了解 MCP Resources 和 Prompts 的实践用法
- [ ] 尝试通过 HTTP SSE 方式部署 MCP 服务

---

